<UserControl x:Class="Colonies.Views.EnvironmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Colonies="clr-namespace:Colonies" xmlns:ViewModels="clr-namespace:Colonies.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance ViewModels:EnvironmentViewModel}">
    
    <UserControl.Resources>
        <Colonies:TerrainToBrushConverter x:Key="TerrainToBrushConverter" />
    </UserControl.Resources>

    <Rectangle Fill="{Binding Terrain, Converter={StaticResource TerrainToBrushConverter}}" />
    
</UserControl>
