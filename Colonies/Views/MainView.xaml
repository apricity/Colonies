<Controls:MetroWindow x:Class="Colonies.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:ViewModels="clr-namespace:Colonies.ViewModels"
        xmlns:Views="clr-namespace:Colonies.Views"
        mc:Ignorable="d" 
        Title="Wacton Colonies" Height="350" Width="300"
        d:DataContext="{d:DesignInstance ViewModels:MainViewModel}"
        ShowTitleBar="True" ShowIconOnTitleBar="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colours.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Viewbox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Stretch="Fill">
            <Views:EcosystemView DataContext="{Binding EcosystemViewModel}"/>
        </Viewbox>

        <!--<Controls:ToggleSwitchButton Grid.Row="1" Grid.Column="0" Command="{Binding ToggleEcosystemCommand}" />-->
        <Controls:ToggleSwitch Grid.Row="1" Grid.Column="0" IsChecked="{Binding IsEcosystemActive}"
                               Margin="5, 5, 5, 5" />

        <Slider Grid.Row="1" Grid.Column="1" Value="{Binding EcosystemTurnInterval}" AutoToolTipPlacement="BottomRight" 
                Minimum="1" Maximum="2000" Margin="5, 5, 5, 5"/>
    </Grid>
    
</Controls:MetroWindow>
