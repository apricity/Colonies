<UserControl x:Class="Colonies.NicheView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Colonies="clr-namespace:Colonies"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Colonies:NicheViewModel}">

    <Border BorderBrush="Black" BorderThickness="1, 1, 0, 0">
        <!-- border on left & top of each item -->
        <Grid Height="30" Width="30">
            <!-- TODO: HABITAT VIEW -->
            <Colonies:HabitatView DataContext="{Binding HabitatViewModel}"/>

            <!-- TODO: ORGANISM VIEW -->
            <Colonies:OrganismView DataContext="{Binding OrganismViewModel}" />
        </Grid>
    </Border>
    
</UserControl>
