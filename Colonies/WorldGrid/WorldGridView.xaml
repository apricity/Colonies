<UserControl x:Class="Colonies.WorldGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Colonies="clr-namespace:Colonies"
             mc:Ignorable="d">
    
        <UserControl.DataContext>
            <Colonies:WorldGridViewModel />
        </UserControl.DataContext>

        <UserControl.Resources>
            <Colonies:CellTypeToBrushConverter x:Key="CellTypeToBrushConverter" />
            <Colonies:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        </UserControl.Resources>

        <Viewbox Stretch="Fill">
            <Border BorderBrush="Black" BorderThickness="0, 0, 1, 1">
                <!-- border on right & bottom of items control -->
                <ItemsControl ItemsSource="{Binding Cells}">
                    <!-- define the control used for the collection of collections -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <!-- define how each collection is presented - a template that is applied to each collection -->
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding}">

                                <!-- define the panel used for the collection of items -->
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"></StackPanel>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <!-- define how each item is presented - a template that is applied to each item -->
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="Black" BorderThickness="1, 1, 0, 0">
                                            <!-- border on left & top of each item -->
                                            <Grid Height="30" Width="30">
                                                <Rectangle Fill="{Binding CellType, Converter={StaticResource CellTypeToBrushConverter}}" />
                                                <Rectangle Height="15" Width="15" Fill="Black"
                                                    Visibility="{Binding HasOccupant, Converter={StaticResource BoolToVisibilityConverter}}">
                                                </Rectangle>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>

                            </ItemsControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </Border>
        </Viewbox>
</UserControl>
