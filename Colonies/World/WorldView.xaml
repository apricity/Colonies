<UserControl x:Class="Colonies.WorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Colonies="clr-namespace:Colonies"
             mc:Ignorable="d">
    
    <!-- initialises the world view model... move this to Application Startup event? -->
    <UserControl.DataContext>
        <Colonies:WorldViewModel />
    </UserControl.DataContext>

    <Viewbox Stretch="Fill">
        <Colonies:EcosystemView DataContext="{Binding Ecosystem}"/>
    </Viewbox>
</UserControl>
